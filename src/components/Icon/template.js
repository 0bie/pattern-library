const icon = 'github';

export const markup = `<header>
  <h1 class="txt--center">Icon</h1>
</header>
<div class="p2">
  <svg class="icon icon--xsm" role="img" aria-labelledby="title" width="30" height="30">
    <title>${icon}</title>
    <desc>A ${icon} icon</desc>
    <use xlink:href=#${icon}></use>
  </svg>
  <svg class="icon icon--sm" role="img" aria-labelledby="title" width="30" height="30">
    <title>${icon}</title>
    <desc>A ${icon} icon</desc>
    <use xlink:href=#${icon}></use>
  </svg>
  <svg class="icon icon--md" role="img" aria-labelledby="title" width="30" height="30">
    <title>${icon}</title>
    <desc>A ${icon} icon</desc>
    <use xlink:href=#${icon}></use>
  </svg>
  <svg class="icon icon--lg" role="img" aria-labelledby="title" width="30" height="30">
    <title>${icon}</title>
    <desc>A ${icon} icon</desc>
    <use xlink:href=#${icon}></use>
  </svg>
  <svg class="icon icon--xl" role="img" aria-labelledby="title" width="30" height="30">
    <title>${icon}</title>
    <desc>A ${icon} icon</desc>
    <use xlink:href=#${icon}></use>
  </svg>
</div>`;